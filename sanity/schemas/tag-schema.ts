const tag = {
  name: "tag",
  title: "Tag",
  type: "object",
  fields: [
    {
      name: "name",
      title: "Name",
      type: "string",
    },
    // You can include additional fields for tags if needed, such as color, description, etc.
  ],
};

export default tag;